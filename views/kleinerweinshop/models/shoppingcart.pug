script(type='text/template', id='model-shoppingcart-template')
  img(src!='<%-item.image%>')
  h3 <%-item.name%>
  div#tags
    <% for(let tag of item.tags){%>
    span#tag.tag <%-tag%>
    <%}%>
  span.price <%-item.price%> €
  input(type='number' value!='<%-amount%>' min='0' max!='<%-item.amount%>')
  i#remove(class='fa fa-trash')
